kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: upload-demo
spec:
  replicas: 2
  template:
    metadata:
      name: upload-demo
      labels:
        app: upload-demo
    spec:
      containers:
      - name: demo
        image: demo
        resources:
          limits:
            memory: "500Mi"
            cpu: "100m"
        imagePullPolicy: Always
        readinessProbe:
          httpGet:
            path: /
            port: 8080
        ports:
        - name: upload-demo
          containerPort: 8080