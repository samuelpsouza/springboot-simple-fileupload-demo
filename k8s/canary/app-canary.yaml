kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: upload-demo
spec:
  replicas: 1
  template:
    metadata:
      name: upload-demo
      labels:
        app: upload-demo
        env: canary
    spec:
      containers:
      - name: demo
        image: 
        resources:
          limits:
            memory: "500Mi"
            cpu: "100m"
        imagePullPolicy: Always
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
        ports:
        - name: upload-demo
          containerPort: 8080